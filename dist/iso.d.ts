export declare function makeISO(inputFolder: string, outputISO: string): void;
declare type Params = {
    input: string;
    out: string;
};
export declare function calculateOutput({ input, out }: Params): string;
export {};
